{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  // Fleet-wide Renovate rollout (WS6), following the Phase 1 canary in
  // LDMB123/bor-radio#54. Where .github/dependabot.yml exists in this repo,
  // it stays in place until Renovate actually lands a real PR here -- then
  // it is removed in a follow-up PR. See PLAN.md guardrail #4
  // (dependency-modernization-2026-07-22/PLAN.md).
  "extends": ["github>LDMB123/renovate-config#v1.0.0"]
}
